<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--suppress MenuTitle - IDs are needed for OSMdroid to find this layout -->
    <item
        android:id="@+id/menu_monitoring"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_button_connect"
        android:icon="@drawable/ic_button_connected"
        android:title="Connect"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_button_disconnect"
        android:icon="@drawable/ic_button_disconnect"
        android:title="Disconnect"
        app:showAsAction="ifRoom" />
</menu>
